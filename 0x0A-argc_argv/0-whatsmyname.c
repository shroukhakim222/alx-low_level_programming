#include <stdio.h>

/**
 * main - print program name
 * @argc: command count
 * @argv: command array
 *
 * Return: always.
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
