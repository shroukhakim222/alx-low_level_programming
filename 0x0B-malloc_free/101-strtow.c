#include "main.h"
#include <stdio.h>

/**
 * **strtow - splits a string into words.
 * @str: string to split
 *
 * Return: pointer to 2d array
 */

char **strtow(char *str)
{
	int i, j;

	if (str == NULL || str == "")
		return (NULL);
}
