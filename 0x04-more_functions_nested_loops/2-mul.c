#include <stdio.h>
#include "main.h"

/**
 * mul - multiplicate two number
 * @a: first number
 * @b: second number
 *
 * Return: multiplication result
 */

int mul(int a, int b)
{
	return (a * b);
}
